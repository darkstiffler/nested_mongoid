source 'https://rubygems.org'


# these are the required gems for launch  / with their version
# remove versions to keep gems updated all the time

gem 'rails', '4.0.0'
gem 'sass-rails', '~> 4.0.0'
gem 'uglifier', '>= 1.3.0'
gem 'mongoid', github: 'mongoid/mongoid'
gem 'coffee-rails', '~> 4.0.0'
gem 'jquery-rails'
gem 'turbolinks'
gem 'jbuilder', '~> 1.2'

# which gems run in dev and test mode

group :development, :test do
	gem 'rspec-rails'
	gem 'database_cleaner'
end

